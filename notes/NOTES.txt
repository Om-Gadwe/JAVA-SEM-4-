JAVA ME SOURCE CODE -> JDK -> BYTE CODE
C++/C ME SOURCE CODE -> MACHINE CODE
COMPILE AND ERROR COMES UNDER JRE( JAVA RUN TIME ENVIORNMENT) 

static data and class information is stored in method area/memory 
objects are stored in heap, instance class are belong to object whereas static variable store only one data that is shared by all object which is stored in method area

*)Static variables are stored separately because they belong to the class and have only one shared copy, while instance variables belong to each object so every object needs its own copy inside heap memory

wrapper classes:-
it provide a way to use primitive data type as object 


1)Command Line Input
Input is given while running the program, from the terminal/command prompt
args[] is a command-line argument array
Values are passed when you run the program
2)Scanner Input
Input is taken during program execution, from the keyboard.

"this" keyword is used to refer to the current object of the class
"super" keyword is used to refer to the immediate parent class

Runtime polymorphism in Java is achieved through method overriding. In this program, the dog class overrides the eat() method of the animal class. When the method is called using a child class object, the JVM decides at runtime which method to execute based on the object type, hence demonstrating runtime polymorphism.

final Variables Definition:
A final variable is a constant whose value cannot be changed after initialization

final Methods Definition:
A final method cannot be overridden by a subclass

final Classes Definition:
A final class cannot be extended by any subclass

Abstract Keyword Definition:
The abstract keyword is used to define classes or methods that are incomplete and must be implemented by a subclass
Key Characteristics of Abstract Methods Points:
-Cannot be instantiated: Abstract methods exist only within abstract classes, which cannot be directly instantiated
-Must be overridden: Subclasses must implement all abstract methods unless they are also abstract classes
-Can coexist with concrete methods: Abstract classes can have both abstract and concrete methods
-Used for polymorphism: Provides a way to achieve runtime polymorphism

Interfaces in Java Definition:
An interface is a blueprint of a class that contains abstract methods

Runtime Polymorphism in Java
It allows a method to be overridden in the subclass and called through a superclass reference.
The method that gets called is determined at runtime, not compile-time

package:Java provides many built-in packages such as:
java.util → Collections, Date & Time
java.io → File handling
java.net → Networking
java.sql → Database handling

An array is a fixed-size, indexed collection of elements of the same data type
ArrayList is a resizable dynamic array in Java (part of java.util package)

Vector is similar to ArrayList but synchronized (thread-safe)
Slower than ArrayList due to synchronization overhead

Automatic memory management in Java
Java’s Garbage Collector (GC) removes unused objects from memory
Prevents memory leaks and improves performance

A String in Java is an immutable sequence of characters
Immutable: Once created, its value cannot be changed

model view controller architechture and monolithic architechture
Model–View–Controller (MVC) is a software design pattern that divides an application into three parts: the Model (data and business logic), the View (user interface), and the Controller (handles requests and connects Model and View). This separation makes the application easier to manage, maintain, and scale. MVC is commonly used in web applications and frameworks.
Monolithic architecture is a system design where the entire application—UI, business logic, APIs, and database access—is built and deployed as one single unit. All components are tightly connected in one codebase. It is simple for small projects but becomes difficult to scale and maintain as the application grows.

PreparedStatement is a JDBC interface used to execute precompiled SQL queries with parameters, improving security and performance
It allows you to write SQL queries with placeholders (?) and then safely insert values into those placeholders before execution
SQL INJECTION: SQL Injection is a type of security attack in which a hacker inserts malicious SQL code into input fields (like login forms) to manipulate or access a database without authorization

We need Tomcat when:
Frontend (HTML / CSS / JavaScript)
↓
Backend (Java)
↓
Database (MySQL)
Because browser cannot directly talk to Java code
It needs a server in between
Tomcat acts as that server
With Tomcat:
User opens http://localhost:8080/login
Tomcat receives request
Calls your Servlet
Servlet connects to MySQL using JDBC
Sends response back to browser